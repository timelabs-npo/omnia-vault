# macOS prototype status

The user discarded all current macOS implementation as the product baseline on 12 September 2026. Preserve existing source for audit. Do not repair, build, relaunch or treat these panes as accepted work. This decision supersedes the implementation directions in earlier UI correction tasks.

Start with [the technical reset](docs/technical-reset/README.md), then its technical contract, LIT admission gate, network contract and requirements register. This package is a proposed contract, not permission to resume product implementation.

The recovered LIT candidate is not frozen. Neither inspected implementation conforms. No compliant LIT publications or accepted chain were produced. An ordinary file digest is not a LIT receipt. Resolve contract and engine qualification through their separate prerequisites before admitting macOS/product implementation.

Preserve current VPN, model sessions and network services. Do not run failure tests on live work. Keep every requested storage provider in beta scope. Timelabs is vendor information only, never navigation. The future main app window must move and resize normally; a fixed popup is not the workspace.
